
//get the array of projects from the homepage and convert from string back to array
/*var projectsArray;

projectsArray =  JSON.parse(localStorage.getItem('projects'));;

console.log(projectsArray);
var projectTitle = $(".title-main h1").text().replace(/\s/g, "");

var currentProjectIndex;
$(projectsArray).each(function (index, item) {
	var title;
	if(title != null){
		title = $(item).attr("title").replace(/\s/g, "");
	}
	
	if(title == projectTitle){
		console.log(index);
		currentProjectIndex = index;
	}

})

var nextProjectIndex = currentProjectIndex + 1;
var lastProjectIndex = currentProjectIndex - 1;


var nextProjectImg = projectsArray[nextProjectIndex].attr("img");
console.log(nextProjectImg);
$(".next .img").attr("src",nextProjectImg);
*/